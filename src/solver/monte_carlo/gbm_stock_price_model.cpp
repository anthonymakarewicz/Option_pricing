#include "solver/monte_carlo/gbm_stock_price_model.h"

/*
namespace OptionPricer {
    BrownianMotionModel::BrownianMotionModel(const std::string &ticker, std::shared_ptr<IMarketData> marketData,
        const int& seed)
        : StockPriceModel(std::move(ticker), std::move(marketData)) {}
}

*/